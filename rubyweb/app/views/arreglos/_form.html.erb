<%= form_for(@arreglo) do |f| %>
  <% if @arreglo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arreglo.errors.count, "error") %> prohibited this arreglo from being saved:</h2>

      <ul>
      <% @arreglo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :numero %><br>
    <%= f.text_field :numero %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
